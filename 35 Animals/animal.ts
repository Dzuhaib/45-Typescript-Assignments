let commonCharacteristic: string = 'They are mammals';
let animals: string[] = ['Lion', 'Dog', 'Goat'];

for (let animal of animals) {
    console.log(`A ${animal.toLowerCase()} would make a great pet.`);
}
console.log(commonCharacteristic);