"use strict";
// Define an array of names
let names = ["Zuhaib", "Afridi", "Shoaib", "Usman", "Eve"];
// Print each person's name one at a time
for (let i = 0; i < names.length; i++) {
    console.log(names[i]);
}
